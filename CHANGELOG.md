## 1.0.2

* Align mobile payload schema with the FDS scoring engine (`security`/`battery`/`network`/`gps`/`screen`/`uptime`, `stableDeviceHash`, `isRooted`).
* Parse `sessionId` from the session response (was `session_id`).
* Send `X-Antifraud-SDK-Version` header.

## 1.0.1

* Move from Cocoapods to  Swift Package Manager.

## 1.0.0

* Initial release of the native iOS CocoaPods SDK.
